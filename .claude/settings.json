{
  "permissions": {
    "defaultMode": "bypassPermissions"
  },
  "hooks": {
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "set -eu; for nb in analysis glm_pricing_models; do p=/workspaces/MotorForsikring/$nb; if [ -f $p.ipynb ] && [ $p.ipynb -nt $p.py ]; then uv run --project /workspaces/MotorForsikring jupytext --to py:percent --output $p.py $p.ipynb; fi; done",
            "statusMessage": "Syncing notebook .py files from .ipynb..."
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Edit|Write|NotebookEdit",
        "hooks": [
          {
            "type": "command",
            "command": "f=$(jq -r '.tool_input.file_path // empty'); case \"$f\" in *.py) uv run --project /workspaces/MotorForsikring ruff format \"$f\" 2>/dev/null || true ;; esac",
            "statusMessage": "Formatting with ruff..."
          }
        ]
      }
    ]
  }
}
